import { Subjects } from '../../subjects';
export interface HardwareUpdatedEvent {
    subject: Subjects.HardwareUpdated;
    data: {
        id: string;
        name: string;
        ip: string;
        protocol?: string;
        port: Number;
        props?: any;
        type: String;
    };
}
