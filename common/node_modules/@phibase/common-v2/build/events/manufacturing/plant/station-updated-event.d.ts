import { Subjects } from '../../subjects';
export interface StationUpdatedEvent {
    subject: Subjects.StationUpdated;
    data: {
        id: string;
        name: string;
        hardwares: string[];
        stations?: string[];
        props?: any;
    };
}
