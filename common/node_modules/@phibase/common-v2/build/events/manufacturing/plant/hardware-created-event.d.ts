import { Subjects } from '../../subjects';
export interface HardwareCreatedEvent {
    subject: Subjects.HardwareCreated;
    data: {
        id: string;
        name: string;
        ip: string;
        protocol?: string;
        port: Number;
        props?: any;
        type: String;
    };
}
