import { Subjects } from '../../subjects';
export interface QualityControlCreatedEvent {
    subject: Subjects.QualityControlCreated;
    data: {
        id: string;
        item: string;
        user: string;
        result?: QualityResult[];
        status: string;
        barcode: string;
        approvedBy: string;
        timestamp: Date[];
        database: string;
    };
}
interface QualityResult {
    testParameter: string;
    alias: string;
    minSpec: number;
    maxSpec: number;
    specification: any[];
    result: string;
    status: string;
    examiner: string;
}
export {};
