import { Subjects } from '../../subjects';
/**
 * eventName    : Log Created
 * source       : all (Manufacture,Distribution,Commerce)
 * description  : used to send data from any point in any domain
 *                to be logged and put into Common/Log
 */
export interface LogCreatedEvent {
    subject: Subjects.LogCreated;
    data: {
        domain: string;
        app: string;
        props: any;
    };
}
