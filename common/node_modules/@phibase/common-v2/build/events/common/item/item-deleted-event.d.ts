import { Subjects } from '../../subjects';
export interface ItemDeletedEvent {
    subject: Subjects.ItemDeleted;
    data: {
        id: string;
        version: number;
        name: string;
        app?: string;
        domain?: string;
        props?: any;
    };
}
