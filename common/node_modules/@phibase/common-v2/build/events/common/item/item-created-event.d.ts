import { Subjects } from '../../subjects';
export interface ItemCreatedEvent {
    subject: Subjects.ItemCreated;
    data: {
        id: string;
        version: number;
        name: string;
        app?: string;
        domain?: string;
        props?: any;
    };
}
