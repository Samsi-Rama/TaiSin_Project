import { Subjects } from '../../subjects';
export interface ItemUpdatedEvent {
    subject: Subjects.ItemUpdated;
    data: {
        id: string;
        version: number;
        name: string;
        app?: string;
        domain?: string;
        props?: any;
    };
}
