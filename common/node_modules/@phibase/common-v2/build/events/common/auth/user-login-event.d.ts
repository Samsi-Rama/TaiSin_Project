import { Subjects } from '../../subjects';
export interface UserLoginEvent {
    subject: Subjects.UserLogin;
    data: {
        id: string;
        version: number;
        email?: string;
        password?: string;
        role?: string;
        props?: any;
    };
}
