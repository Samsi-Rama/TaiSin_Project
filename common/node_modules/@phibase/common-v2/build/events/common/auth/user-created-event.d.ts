import { Subjects } from '../../subjects';
export interface UserCreatedEvent {
    subject: Subjects.UserCreated | Subjects.CustomerCreated | Subjects.SalesPersonCreated;
    data: {
        id: string;
        version: number;
        email?: string;
        password?: string;
        role?: string;
        props?: any;
    };
}
