import { Subjects } from '../../subjects';
export interface UserUpdatedEvent {
    subject: Subjects.UserUpdated | Subjects.CustomerUpdated | Subjects.SalesPersonUpdated;
    data: {
        id: string;
        version: number;
        email?: string;
        password?: string;
        role?: string;
        props?: any;
    };
}
