import { Subjects } from '../../subjects';
export interface DeliveryUpdatedEvent {
    subject: Subjects.DeliveryUpdated;
    data: {
        id: string;
        version: number;
        deliveryNumber: string;
        packing: string;
        address: string;
        receiver: string;
        courier: string;
        coDriver: string;
        option?: string;
        detail?: string;
        status: string;
        order: string;
        props: any;
        itemList: Array<Items>;
    };
}
interface Items {
    item: any;
    qty: number;
}
export {};
