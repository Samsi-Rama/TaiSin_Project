import { Subjects } from '../../subjects';
export interface VehicleUpdatedEvent {
    subject: Subjects.VehicleUpdated;
    data: {
        id: string;
        version: number;
        licensePlate: string;
        type: string;
        vehicleModel: string;
        capacityUnit: string;
        maxCapacity: number;
        dimensionUnit: string;
        width: number;
        length: number;
        height: number;
    };
}
