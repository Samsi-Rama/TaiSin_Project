import { Subjects } from '../../subjects';
export interface PickingCreatedEvent {
    subject: Subjects.PickingCreated;
    data: {
        id: string;
        version: number;
        warehouse: string;
        order: string;
        type: string;
        status: string;
    };
}
