import { Subjects } from '../../subjects';
export interface PickingUpdatedEvent {
    subject: Subjects.PickingUpdated;
    data: {
        id: string;
        version: number;
        warehouse: string;
        order: string;
        type: string;
        status: string;
    };
}
