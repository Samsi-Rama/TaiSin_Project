import { Subjects } from '../../subjects';
export interface DelivToCommerceUpdatedEvent {
    subject: Subjects.DelivToCommerceUpdated;
    data: {
        id: string;
        version: number;
        address: string;
        status: string;
        order: string;
        receiver: string;
        courier?: string;
        itemsList: Array<Items>;
        deliveryNumber: string;
        detail?: string;
        props: any;
    };
}
interface Items {
    item: any;
    qty: number;
}
export {};
