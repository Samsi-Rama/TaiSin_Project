import { Subjects } from '../../subjects';
export interface MovingCreatedEvent {
    subject: Subjects.MovingCreated;
    data: {
        id: string;
        version: number;
        name: string;
        location: string;
        item: WarehouseItem[];
        props: any;
    };
}
interface WarehouseItem {
    id: string;
    qty: number;
}
export {};
