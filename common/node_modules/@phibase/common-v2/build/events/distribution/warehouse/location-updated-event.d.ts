import { Subjects } from '../../subjects';
export interface LocationUpdatedEvent {
    subject: Subjects.LocationUpdated;
    data: {
        id: string;
        version: number;
        name: string;
        address: string;
        props: any;
    };
}
