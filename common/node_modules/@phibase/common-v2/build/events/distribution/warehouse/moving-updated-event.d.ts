import { Subjects } from '../../subjects';
export interface MovingUpdatedEvent {
    subject: Subjects.MovingUpdated;
    data: {
        id: string;
        version: number;
        name: string;
        location: string;
        item: WarehouseItem[];
        props: any;
    };
}
interface WarehouseItem {
    id: string;
    qty: number;
}
export {};
