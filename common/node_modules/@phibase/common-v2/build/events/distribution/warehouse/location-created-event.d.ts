import { Subjects } from '../../subjects';
export interface LocationCreatedEvent {
    subject: Subjects.LocationCreated;
    data: {
        id: string;
        version: number;
        name: string;
        address: string;
        props: any;
    };
}
