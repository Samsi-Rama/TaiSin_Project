import { Request, Response, NextFunction } from 'express';
declare global {
    namespace Express {
        interface Request {
            mongoQuery?: any;
        }
    }
}
export declare const mongoQuery: (req: Request, res: Response, next: NextFunction) => void;
